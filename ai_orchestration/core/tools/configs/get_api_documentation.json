{
  "tool_id": "get_api_documentation",
  "description": "AUTO-DISCOVERY: Get ExperimentOps API documentation from API.json (generated by inspect module). Returns method signatures, parameters, and usage examples including the NEW beautiful API (ops.assets.apple.position). Pure MOP - reads from generated docs!",
  "type": "function_tool",
  "parameters": {},
  "returns": {
    "methods": "object mapping method names to their documentation",
    "new_api": "object describing the new beautiful asset access API",
    "workflow": "array of workflow steps"
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "methods": {"type": "object"},
      "new_api": {"type": "object"},
      "workflow": {"type": "array"}
    },
    "required": ["methods", "new_api", "workflow"]
  },
  "workflow_hints": {
    "stage": "planning",
    "order": 5,
    "when_to_use": "After seeing examples, review the complete API documentation to understand all available methods and their signatures",
    "outputs_feed": ["create_scene_script"]
  },
  "examples": [
    {
      "description": "Get API docs",
      "input": {},
      "output_summary": "Returns 48 ExperimentOps methods with signatures, parameters, and the new beautiful asset access API",
      "use_case": "To learn method signatures and parameters before writing scene scripts"
    }
  ],
  "best_practices": [
    "Use the NEW beautiful API when possible: ops.assets.apple.position instead of dict lookups",
    "Reference this documentation for correct method signatures and parameter names"
  ]
}
